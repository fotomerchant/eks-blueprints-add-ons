{{- if .Values.apps.ai.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.ai.name }}
  annotations:
    "helm.sh/hook-weight": "-1"
  namespace: fmhv
  labels:
    app: {{ .Values.apps.ai.name }}
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9000
    - name: https
      port: 443
      protocol: TCP
      targetPort: 9000
  selector:
    app: {{ .Values.apps.ai.name }}
{{- end }}
