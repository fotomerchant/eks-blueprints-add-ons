{{- if .Values.apps.hvt.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.apps.hvt.name }}
  namespace: fmhv
  annotations:
    {{- printf .Values.apps.ingress_definition_dns | nindent 4 }}
    external-dns.alpha.kubernetes.io/set-identifier: "{{ .Values.clusterName }}"
    external-dns.alpha.kubernetes.io/aws-weight: "{{ .Values.route53_weight }}"
spec:
  tls:
  - hosts:
    - hvt.{{ .Values.awsAccountAlias }}.fotomerchanthv.com
    secretName: cert-hvt
  rules:
  - host: hvt.{{ .Values.awsAccountAlias }}.fotomerchanthv.com
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.apps.hvt.name }}
              port:
                number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hvt-web-star
  namespace: fmhv
  annotations:
    {{- printf .Values.apps.ingress_definition_cluster_dns | nindent 4 }}
    external-dns.alpha.kubernetes.io/set-identifier: "{{ .Values.clusterName }}"
    external-dns.alpha.kubernetes.io/aws-weight: "{{ .Values.route53_weight }}"
spec:
  tls:
    - hosts:
        - '*.{{ .Values.awsAccountAlias }}.fotomerchanthv.com'
      secretName: cert-hvt-star
  rules:
    - host: '*.{{ .Values.awsAccountAlias }}.fotomerchanthv.com'
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.apps.hvt.name }}
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hvt-web-cluster
  namespace: fmhv
  annotations:
    {{- printf .Values.apps.ingress_definition_cluster_dns | nindent 4 }}
    external-dns.alpha.kubernetes.io/set-identifier: "{{ .Values.clusterName }}"
    external-dns.alpha.kubernetes.io/aws-weight: "100"
spec:
  tls:
    - hosts:
        - '*.{{ .Values.awsAccountAlias }}.{{ .Values.clusterName }}.fotomerchanthv.com'
      secretName: cert-hvt-cluster
  rules:
    - host: '*.{{ .Values.awsAccountAlias }}.{{ .Values.clusterName }}.fotomerchanthv.com'
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.apps.hvt.name }}
                port:
                  number: 80
{{- end }}
