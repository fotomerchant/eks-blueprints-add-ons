{{- if .Values.apps.hvt.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.hvt.name }}
  namespace: fmhv
  labels:
    app: {{ .Values.apps.hvt.name }}
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9000
    - name: https
      port: 443
      protocol: TCP
      targetPort: 9000
  selector:
    app: {{ .Values.apps.hvt.name }}
{{- end }}
