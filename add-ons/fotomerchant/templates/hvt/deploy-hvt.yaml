{{- if .Values.apps.hvt.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.apps.hvt.name }}
  namespace: fmhv
  labels:
    app: {{ .Values.apps.hvt.name }}
    group: sync
spec:
  replicas: {{ .Values.min_node_size }}
  selector:
    matchLabels:
      app: {{ .Values.apps.hvt.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.apps.hvt.name }}
    spec:
      affinity:
        {{ if .Values.apps.mi.opn }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - {{ .Values.apps.mi.name }}
              topologyKey: "kubernetes.io/hostname"
        {{ end }}
      serviceAccountName: fmhv-app
      containers:
      - name: app
        image: "{{ .Values.apps.repository }}apphvtbundle:{{ .Values.apps.hvt.revision }}"
        imagePullPolicy: {{ .Values.apps.image_pull_policy }}
        args:
        - -o
        - appnginx
        env:
        - name: ACCOUNT_ALIAS
          value: "{{ .Values.awsAccountAlias }}"
        - name: APP_NAME
          value: "hvt"
        - name: AWS_REGION
          value: "{{ .Values.awsRegion }}"
        volumeMounts:
        - name: symfonyenv-volume
          mountPath: /usr/src/app/.env
          subPath: symfony.hvt.env
        resources:
          requests:
            cpu: "600m"
            memory: "300Mi"
      imagePullSecrets:
        - name: eks-argocd-github-packages-token
      volumes:
        - name: config-volume
          configMap:
            name: configuration
        - name: symfonyenv-volume
          configMap:
            name: configuration
      restartPolicy: Always
{{- end }}
